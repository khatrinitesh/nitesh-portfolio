import { Github, Linkedin, Mail, MessageCircle } from "lucide-react";
import type { SocialIconsType } from "../interface/interface";

export const SocialIcons: SocialIconsType = {
  github: Github,
  linkedin: Linkedin,
  email: Mail,
  whatsapp: MessageCircle,
};
